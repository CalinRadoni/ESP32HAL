on: [push]

jobs:
  builder:
    name: Builder for test project
    runs-on: ubuntu-20.04

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - run: pwd

      - name: Build
        uses: CalinRadoni/esp-idf-v4.1-action@v0.1
        with:
          src-directory: 'test'
